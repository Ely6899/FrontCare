<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FrontCareProject"  parent="Theme.AppCompat.Light.NoActionBar"><!--parent="Theme.Material3.DayNight.NoActionBar">-->
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>


    <style name="Theme.FrontCareProject" parent="Base.Theme.FrontCareProject">
        <item name="android:windowBackground">@color/backGroundColor</item>
        <item name="android:buttonStyle">@style/AppButton</item>
        <item name="android:spinnerStyle">@style/AppSpinner</item>

    </style>

    <!-- Button style -->
    <style name="AppButton" parent="Widget.AppCompat.Button">
        <item name="android:textColor">@color/buttonTextColor</item>
        <item name="android:backgroundTint">@color/buttonColor</item>
        <!--<item name="android:background">@drawable/capsule_button_background</item> -->
        <!-- Add other button attributes as needed -->
    </style>

    <style name="AppTable">
        <item name="android:background">@color/tablesBackgroundColor</item>
        <!-- Add other table attributes as needed -->
    </style>
    <style name="AppSpinner" parent="Widget.AppCompat.Spinner">
<!--        <item name="android:background">@color/spinnerColor</item>-->
        <item name="android:backgroundTint">@color/spinnerColor</item>

        <!-- Add other spinner attributes as needed -->
    </style>



</resources>